<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <item tools:ignore="MenuTitle">
        <menu>
            <item
                android:id="@+id/addFoodD"
                android:icon="@drawable/ic_food_bar"
                android:title="Add Foods" />
            <item
                android:id="@+id/adminPanel"
                android:icon="@drawable/ic_person"
                android:title="Admin Panel" />
            <item
                android:id="@+id/salesRepo"
                android:icon="@drawable/ic_sales_repo"
                android:title="Sales Report" />
        </menu>
    </item>
    <group>
        <item
            android:id="@+id/premium"
            android:icon="@drawable/restora_logo"
            android:title="Restora POS Premium" />
    </group>
</menu>